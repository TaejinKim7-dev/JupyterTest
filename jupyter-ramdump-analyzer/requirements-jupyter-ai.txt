# JupyterLab 핵심
jupyterlab>=4.0.0
ipython>=8.0.0
ipywidgets>=8.0.0

# jupyter-ai 3.x + Jupyternaut (Chat UI → LiteLLM → OpenRouter 경로)
jupyter-ai[jupyternaut]==3.0.0
# jupyter-ai-magics 제거: langchain<0.4.0 요구 → jupyter-ai[jupyternaut]의 langchain 1.x와 충돌
# %%ai 노트북 매직은 litellm을 직접 wrapping해서 사용 (notebooks/ 참고)

# LiteLLM: Jupyternaut Chat UI + 노트북 %%ai 커스텀 매직 양쪽에 사용
litellm

# LangChain (분석 파이프라인 src/llm_assistant.py용)
langchain-openai>=0.3.0

# 실시간 협업 (없으면 globalAwareness 404 경고 발생)
jupyter-collaboration
# 파일 ID 서비스 (없으면 notebook File ID error 발생)
jupyter-server-fileid

# 유틸
python-dotenv>=1.0.0
requests>=2.31.0
