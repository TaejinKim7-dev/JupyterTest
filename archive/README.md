# Archive

`archive/` 는 현재 개발 흐름에서 직접 사용하지 않지만 보관이 필요한 파일을 두는 영역입니다.

## 현재 구성

- `git-metadata/`
  - `jupyter-ramdump-analyzer.git`

위 폴더는 루트 Git 저장소로 통합하기 전에 `jupyter-ramdump-analyzer/` 내부에 있던 기존 `.git` 디렉터리를 보관한 것입니다.
